import {layout,esc,json,sourcePanel,comparePath,compareTable,schemaBreadcrumb} from './editorial.mjs';
import {quoteApp} from './booking-math.mjs';

const money=n=>'$'+n.toFixed(2);
export const API_DOC='https://help.cowlendar.com/how-to-integrations/connect-cowlendar-to-crm-erp-or-app-public-api-webhooks';
const baseScenario={bookings:20,value:40,features:['bundles','deposits'],bundleShare:100};
export function quoteCard(app,quote){return `<article class="quote-card ${app.key==='cowlendar'?'quote-own':''}" data-quote="${app.key}"><div class="quote-brand"><img src="/assets/apps/${app.key}-icon.webp" width="40" height="40" alt=""><strong>${esc(app.name)}</strong>${app.key==='cowlendar'?'<span>Our pick</span>':''}</div><p class="quote-total">${quote?money(quote.total):'Confirm plan'}${quote?'<small>/ month</small>':''}</p><p class="quote-plan">${quote?esc(quote.plan)+' · '+money(quote.base)+' plan'+(quote.usage?' + '+money(quote.usage)+' usage':''):'No verified price for this exact feature combination.'}</p><p class="quote-year">${quote?money(quote.total*12)+' over 12 months at this monthly rate':''}</p><a href="${app.listing}">Official pricing ↗</a></article>`;}
export function budgetWidget(apps){const choices=[['bundles','deposits'],['bundles'],['google','deposits'],['google']];const selected=choices.find(features=>quoteApp(apps[1],{...baseScenario,features}))||['google'];const scenario={...baseScenario,features:selected};return `<section class="budget-studio wrap" id="budget"><div class="section-heading"><span class="eyebrow">YOUR BUSINESS. YOUR ACTUAL APP BUDGET.</span><h2>What would <em>you</em> pay?</h2><p>Pick the features you need. See the lowest documented plan that covers them.</p></div><form class="app-budget-form" data-apps="${esc(JSON.stringify(apps))}"><div class="budget-inputs"><label><span>Bookings per month</span><input type="number" name="bookings" min="0" max="100000" step="1" value="20" required></label><label><span>Average booking value (USD)</span><input type="number" name="value" min="0" max="1000000" step="0.01" value="40" required></label><label><span>Revenue from bundles (%)</span><input type="number" name="bundleShare" min="0" max="100" value="100" required><small>Used only for listed bundle fees.</small></label></div><fieldset><legend>Features your plan needs</legend>${[['google','Google Calendar'],['bundles','Booking packs / bundles'],['deposits','Booking deposits'],['klaviyo','Klaviyo']].map(([key,label])=>`<label class="feature-chip"><input type="checkbox" name="features" value="${key}" ${scenario.features.includes(key)?'checked':''}><span>${label}</span></label>`).join('')}</fieldset><p class="budget-revenue" data-revenue>20 bookings × $40.00 = <strong>$800.00 monthly booking revenue</strong></p><p class="form-error" data-budget-error hidden role="status"></p><div class="quote-grid" aria-live="polite">${apps.map(a=>quoteCard(a,quoteApp(a,scenario))).join('')}</div><p class="budget-verdict" data-budget-verdict></p></form><p class="budget-fineprint">Monthly USD estimates for one service, one location and one operator. Excludes tax, Shopify/payment fees, SMS/WhatsApp, extra staff logins and optional add-ons. Annual totals use 12 monthly payments, not annual-plan discounts. “Confirm plan” means the exact combination or required volume is not established in our sources, not that the app cannot support it. Cowlendar’s free-plan allowance and revenue tiers apply. <a href="/pricing">Full Cowlendar pricing</a> · <a href="#sources">Sources & assumptions</a></p><noscript><p>The example above uses 20 bookings at $40 and the selected features. Enable JavaScript to adjust it.</p></noscript></section>`;}

export function advancedSection(){return `<section class="power-section wrap"><div class="section-heading"><span class="eyebrow">CUTE NAME. SERIOUS BOOKING ENGINE.</span><h2>A little more <em>moo</em>.<br>A lot more you can do.</h2><p>Your next offer should be a new service, not a new booking system.</p></div><div class="power-grid"><article class="power-card power-subscriptions"><span class="power-symbol" aria-hidden="true">↻</span><h3>Make the next visit part of the plan.</h3><p>Sell recurring service subscriptions and booking bundles. Give customers a reason to return for the next class, coaching session or treatment.</p><div class="mini-credits" aria-label="Example of a six-session pack"><span>6-session pack</span><div>✓ &nbsp; ✓ &nbsp; ✓ &nbsp; ○ &nbsp; ○ &nbsp; ○</div><small>Illustrative workflow</small></div></article><article class="power-card"><span class="power-symbol" aria-hidden="true">⌘</span><h3>Your bookings. Connected to your business.</h3><p>The public API reads services and bookings, creates manual bookings and can redeem a subscription credit. Connect a custom tool or reporting workflow.</p><a href="${API_DOC}">Explore the API (beta) ↗</a></article><article class="power-card"><span class="power-symbol" aria-hidden="true">↗</span><h3>React to what happens next.</h3><p>Eight webhook events cover booking creation, confirmation, rejection, cancellation, rescheduling, attendance and two subscription events.</p><p class="power-detail">Signed payloads · delivery retries · test events</p><a href="${API_DOC}">See webhook events (beta) ↗</a></article><article class="power-card power-resources"><span class="power-symbol" aria-hidden="true">▦</span><h3>Book the person. The place. The equipment.</h3><p>Manage service, teammate or equipment availability. Handle participant categories, group capacity and multi-day bookings alongside regular appointments.</p><a href="/features">Explore all features →</a></article></div><p class="power-caption">API & webhooks are documented as beta. API-created bookings do not create a Shopify order. Available-slot lookup and booking update/delete endpoints are not currently provided. Confirm beta access with our team. <a href="${API_DOC}">Read the current documentation</a>.</p></section>`;}

export function richComparisonPage(app,own,all){
 const third=all.find(a=>a.key===app.third), compared=[own,app,third];
 const faq=[['Does Cowlendar have a free plan?','Yes. Cowlendar has a free plan with all features. Usage allowances apply; the pricing page shows the limits and revenue-based paid tiers.'],[`Is Cowlendar cheaper than ${app.name}?`,`It depends on the features and booking revenue. Use the calculator above: it compares the lowest documented plan for your selected features and shows the assumptions, rather than comparing unrelated starting prices.`],['Does Cowlendar support subscriptions, an API and webhooks?','Yes. Recurring service subscriptions and bundles are supported. The public API and webhooks are documented as beta, including subscription-credit bookings and eight webhook events. Review the linked API limitations before planning an integration.'],[`When should I choose ${app.name} instead?`,app.bestFor+' '+app.tradeoff]];
 const body=`<main id="content"><section class="comparison-hero compare-v2 wrap"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="/">Home</a><span>/</span><a href="/compare">Compare booking apps</a><span>/</span>${esc(app.name)}</nav><div class="versus-logos"><img src="/assets/apps/cowlendar-icon.webp" width="58" height="58" alt="Cowlendar"><span>vs</span><img src="/assets/apps/${app.key}-icon.webp" width="58" height="58" alt="${esc(app.name)}"></div><span class="eyebrow">COWLENDAR VS ${esc(app.name.toUpperCase())} · 2026</span><h1>Cowlendar vs ${esc(app.name)}.<br><em>${esc(app.headline)}</em></h1><p class="article-deck">${esc(app.positioning)}</p><div class="compare-actions"><a class="cw-button" href="${own.listing}">Start for Free ↗</a><a class="text-link" href="#budget">Calculate your price ↓</a></div><p class="hero-free"><span aria-hidden="true">✓</span> Free plan. All features. <a href="/pricing">See plan details</a></p><div class="comparison-stamp"><span>Checked Sep 22, 2026</span><a href="#sources">Official sources</a><span>Published by Cowlendar</span></div></section><section class="value-ribbon wrap" aria-label="Cowlendar highlights"><div><strong>All features</strong><span>available from the free plan</span></div><div><strong>4.9 / 5</strong><span>2,236 Shopify reviews · checked Sep 22</span></div><div><strong>24/7</strong><span>live chat with our team</span></div><div><strong>One app</strong><span>appointments, classes & rentals</span></div></section>${budgetWidget(compared)}<section class="comparison-section wrap"><div class="section-heading"><span class="eyebrow">THE DETAIL THAT MAKES THE DIFFERENCE</span><h2>Compare the capabilities.<br><em>Then compare their plans.</em></h2><p>A checkmark means the feature is documented. The note tells you where to find it.</p></div>${compareTable(own,app)}</section>${advancedSection()}${app.key==='meety'?`<section class="developer-note wrap"><span class="eyebrow">A CLOSER LOOK · COWLENDAR & MEETY</span><h2>Both automate. Compare the actual events.</h2><p>Meety documents booking-created, updated, cancelled and rescheduled webhooks. Cowlendar additionally documents attendance changes, subscription creation and failed subscription billing among its eight events. That gives you specific triggers to evaluate for a CRM, class-pass business or billing follow-up.</p><p>Meety’s API appears on its $47 Enterprise upgrade. Its webhook guide uses the older “Premium” plan name, so confirm the current tier with Meety. Cowlendar’s API and webhooks are beta; verify access and endpoint requirements with our team.</p><a href="${API_DOC}">Cowlendar API & webhook guide ↗</a> <a href="${app.extraSources[0][1]}">Meety webhook guide ↗</a></section>`:''}<section class="decision-grid wrap"><article><span class="eyebrow">WHY WE RECOMMEND COWLENDAR</span><h2>Start with the whole booking toolkit.</h2><p>${esc(own.bestFor)}</p><p>Test deposits, bundles and calendar connections together. Bring in the beta API and webhooks when your operation needs a custom connection.</p><a class="cw-button" href="${own.listing}">Start for Free ↗</a></article><article><span class="eyebrow">WHEN ${esc(app.name.toUpperCase())} MAKES SENSE</span><h2>${esc(app.bestFor)}</h2><p>${esc(app.tradeoff)}</p><a class="text-link" href="${app.listing}">See the official ${esc(app.name)} listing ↗</a></article></section><section class="third-option wrap"><img src="/assets/apps/${third.key}-icon.webp" width="64" height="64" alt=""><div><span class="eyebrow">ONE MORE FOR YOUR SHORTLIST</span><h2>Also consider ${esc(third.name)}.</h2><p>${esc(third.focus)} ${esc(third.bestFor)}</p></div><a class="text-link" href="${comparePath(third)}">Compare ${esc(third.name)} →</a></section><section class="comparison-section wrap"><div class="section-heading"><span class="eyebrow">PLAN BY PLAN</span><h2>No mystery upgrade.</h2><p>All listed monthly tiers, including the advanced plans mentioned in the Shopify listing.</p></div><div class="pricing-comparison">${[own,app].map(a=>`<article><h3>${esc(a.name)}</h3><p>${esc(a.model)}</p><dl>${a.plans.map(([n,p,d])=>`<div><dt>${esc(n)} <strong>${esc(p)}</strong></dt><dd>${esc(d)}</dd></div>`).join('')}</dl><a href="${a.listing}">Verify current pricing ↗</a></article>`).join('')}</div><p class="budget-note">${esc(app.tradeoff)} ${esc(own.tradeoff)}</p></section><section class="product-evidence wrap"><div><span class="eyebrow">SEE THE PRODUCT</span><h2>${esc(app.name)}, on screen.</h2><p>A product image published by the app’s developer on Shopify. Compare the real booking journey alongside the feature list.</p><a href="${app.listing}" class="text-link">Open the listing and demo ↗</a></div><figure><img src="/assets/apps/${app.key}-screen.webp" width="1280" height="720" loading="lazy" decoding="async" alt="${esc(app.name)} booking product shown on its official Shopify listing"><figcaption>Source: <a href="${app.listing}">${esc(app.name)} on Shopify</a>.</figcaption></figure></section><section class="comparison-section wrap"><div class="section-heading"><span class="eyebrow">MAKE THE SWITCH WITH CONFIDENCE</span><h2>Bring your hardest booking to the trial.</h2></div><div class="scenario-grid">${app.scenarios.map((s,i)=>`<article><span class="scenario-number">0${i+1}</span><p>${esc(s)}</p></article>`).join('')}</div><div class="comparison-faq"><h2>Your questions, answered.</h2>${faq.map(([q,a])=>`<details><summary>${esc(q)}</summary><p>${esc(a)}</p></details>`).join('')}</div><div class="comparison-next"><a href="/${app.key}-alternatives">${esc(app.name)} alternatives →</a><a href="/compare">All ${all.length-1} comparisons →</a></div>${sourcePanel(compared)}</section></main>`;
 return layout({title:`Cowlendar vs ${app.name} (2026): Features & Real Costs`,description:`Compare Cowlendar vs ${app.name}: calculate your monthly app cost, compare subscriptions, bundles, deposits and integrations, and find your best Shopify booking fit.`,path:comparePath(app),body,extraClass:'comparison-upgrade',image:'/assets/blog/01-service-booking.webp',schema:[schemaBreadcrumb([['Home','/'],['Compare booking apps','/compare'],[`Cowlendar vs ${app.name}`,comparePath(app)]]),{'@context':'https://schema.org','@type':'FAQPage',mainEntity:faq.map(([q,a])=>({'@type':'Question',name:q,acceptedAnswer:{'@type':'Answer',text:a}}))}]}).replace('</head>','<link rel="stylesheet" href="/growth.css"></head>').replace('</body>','<script type="module" src="/growth.js"></script></body>');
}
